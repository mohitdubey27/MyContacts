import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    height: 42,
    justifyContent: 'center',
    borderRadius: 4,
    paddingHorizontal: 5,
    marginVertical: 10,
    alignItems: 'center',
  },
  loaderSection: {
    flexDirection: 'row',
  },
});
